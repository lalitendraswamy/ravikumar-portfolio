/**
 * ui/index.ts
 *
 * This folder is reserved for reusable, generic UI primitive components
 * that are shared across the entire application (e.g., Button, Modal,
 * Tooltip, Badge, Card, Spinner, etc.).
 *
 * Usage example:
 *   import { Button } from '@/components/ui/Button';
 *
 * Add individual component files here as the project grows,
 * for example:
 *   - Button.tsx
 *   - Modal.tsx
 *   - Badge.tsx
 *   - Spinner.tsx
 */

export { };
