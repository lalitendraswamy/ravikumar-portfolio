/**
 * src/lib/index.ts
 *
 * This folder contains shared helper functions, utility scripts,
 * and configurations for third-party libraries used across the application.
 *
 * Typical contents include:
 *   - utils.ts          — General-purpose utility functions
 *   - constants.ts      — App-wide constants (e.g., site name, contact info)
 *   - validations.ts    — Form validation helpers
 *   - formatters.ts     — Date/number/currency formatters
 *   - api-client.ts     — Base API client setup (e.g., axios instance)
 *
 * Usage example:
 *   import { formatDate } from '@/lib/formatters';
 */

export { };
